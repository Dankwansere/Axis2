export enum ErrorMessage {
    INVALID_LOGIN = 'Invalid credentials entered, please try again',
    USERNAME_TAKEN = 'Sorry, that username is already taken',
    EMAIL_TAKEN = 'Sorry, that email is already taken'
}
